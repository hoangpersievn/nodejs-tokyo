h1 list users
a(href="/user/create") add
form(action='/user/search', method="GET")
    input(type="text", name="q")
    button search
ul 
    each user in users
        li
            span= user.name
            span &nbsp;
            a(href='/user/' + user.id) view